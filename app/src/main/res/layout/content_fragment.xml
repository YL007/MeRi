<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <android.support.v4.view.ViewPager
        android:id="@+id/viewpager"
        android:layout_weight="1"
        android:layout_width="match_parent"
        android:layout_height="0dp"/>

    <RadioGroup
        android:id="@+id/radioGroup"
        android:background="@drawable/radiogroup"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="80dp">
        <RadioButton
            android:id="@+id/rd_news"
            android:drawableTop="@drawable/rd_news_selector"
            android:gravity="center"
            android:text="每日益读"
            style="@style/style_button_subject"
            />
        <RadioButton
            android:id="@+id/rd_pic"
            android:drawableTop="@drawable/rd_pic_selector"
            android:gravity="center"
            android:text="每日美图"
            style="@style/style_button_subject"
            />
        <RadioButton
            android:id="@+id/rd_weather"
            android:drawableTop="@drawable/rd_weather_selector"
            android:gravity="center"
            android:text="每日天气"
            style="@style/style_button_subject"
            />
        <RadioButton
            android:id="@+id/rd_me"
            android:drawableTop="@drawable/rd_me_selector"
            android:gravity="center"
            android:text="我"
            style="@style/style_button_subject"
            />
    </RadioGroup>
</LinearLayout>